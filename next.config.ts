import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ['http://192.168.0.82:3000', 'http://localhost:3000'],
  /* otras opciones de configuración aquí */
};

export default nextConfig;
